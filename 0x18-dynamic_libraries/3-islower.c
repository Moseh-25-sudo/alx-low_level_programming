#include "main.h"
/**
 * _islower - program checks for lowercase charscters
 * @c: the character to check
 * Return: 1 if c is lowercase, and 0 if it's not
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
